# 22. M3.1.5 — 캡처 인라인 뷰어

- **상태**: 구현 착수 (2026-04-27)
- **이전 단계**: [21-m3.1.4-cancel-and-new-execution.md](21-m3.1.4-cancel-and-new-execution.md)
- **다음 단계**: M3.1.6 — Vercel 배포 + Railway PUBLIC_BASE_URL 정착

## 목표

상세 페이지의 캡처 섹션이 현재는 메타 (step / size) 만 보여줌. 실제 PNG 를 인라인으로 표시해 사용자가 실패 시점 화면을 바로 확인할 수 있게.

M3.1.5 완료 조건:

- 캡처 섹션이 그리드 뷰. 카드 하나 = step + 썸네일 + 크기. 클릭 시 새 탭에서 원본.
- `<img src="/v0/captures/{id}">` 로 직접 fetch (Next.js rewrite + 쿠키 자동).
- 이미지 fetch 실패 (Railway ephemeral FS 의 410 Gone) 시 "이미지 사라짐" placeholder.
- 진행 중 (non-terminal) execution 은 captures list 도 3초 polling 으로 자동 갱신.
- typecheck/build PASS, 코어 25/25.

## 사용자 결정 (이전 세션 합의 그대로 적용)

| # | 항목 | 결정 |
|---|---|---|
| 1 | 표시 형태 | 카드 그리드 (sm:2열 / lg:3열). 카드: thumb + step·size 라벨. 클릭 시 새 탭 원본. |
| 2 | fetch 방식 | `<img src="/v0/captures/{id}">` — Next.js rewrite proxy + same-origin 쿠키 |
| 3 | client polling | non-terminal status 일 때만 3초 polling, terminal 도달 시 자동 정지 (LogsTail 와 동일 패턴) |
| 4 | 410 / load 실패 처리 | `onError` → fallback "이미지 사라짐" 메시지 |
| 5 | 이미지 보호 | aspect-[16/10] 컨테이너 + object-contain 으로 잘림 방지 |
| 6 | 다중 agent UX | 범위 밖 (M3.1.6 으로 미룸) |

## 백엔드

**무변경**. M3.1.3 에서 `GET /v0/captures/{capture_id}` 와 `GET /v0/executions/{id}/captures` 가 이미 `current_subject` 사용 → 쿠키 인증으로 OK.

## 웹

### 신규 컴포넌트

```
packages/web/components/captures-grid.tsx   # client, polling + img + 410 fallback
```

### 변경

```
packages/web/app/executions/[id]/page.tsx   # 캡처 섹션 → <CapturesGrid>
```

### 핵심 코드

```typescript
"use client";
export function CapturesGrid({
  executionId,
  initialStatus,
  initialItems,
}: {
  executionId: string;
  initialStatus: ExecutionStatus;
  initialItems: Capture[];
}) {
  const [items, setItems] = useState(initialItems);
  const [status, setStatus] = useState(initialStatus);
  const tickRef = useRef(0);

  useEffect(() => {
    if (TERMINAL_STATUSES.has(status)) return;
    const id = setInterval(async () => {
      tickRef.current += 1;
      const my = tickRef.current;

      const stRes = await apiFetch<{ status: ExecutionStatus }>(
        `/v0/executions/${encodeURIComponent(executionId)}`,
      );
      if (my !== tickRef.current) return;
      if (stRes.status === 200 && stRes.data) setStatus(stRes.data.status);

      const cRes = await apiFetch<CaptureListResponse>(
        `/v0/executions/${encodeURIComponent(executionId)}/captures`,
      );
      if (my !== tickRef.current) return;
      if (cRes.status === 200 && cRes.data) setItems(cRes.data.items);
    }, 3000);
    return () => clearInterval(id);
  }, [status, executionId]);

  if (items.length === 0) {
    return <p className="text-sm text-gray-500">캡처 없음</p>;
  }

  return (
    <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-3">
      {items.map((c) => (
        <CaptureCard key={c.capture_id} capture={c} />
      ))}
    </div>
  );
}

function CaptureCard({ capture }: { capture: Capture }) {
  const [missing, setMissing] = useState(false);
  const url = `/v0/captures/${encodeURIComponent(capture.capture_id)}`;
  return (
    <a
      href={url}
      target="_blank"
      rel="noreferrer"
      className="block rounded-md border border-gray-200 bg-white overflow-hidden hover:border-gray-300"
    >
      <div className="aspect-[16/10] bg-gray-100 flex items-center justify-center">
        {missing ? (
          <span className="text-xs text-gray-400">이미지 사라짐</span>
        ) : (
          <img
            src={url}
            alt={`step ${capture.step_id ?? ""}`}
            onError={() => setMissing(true)}
            className="w-full h-full object-contain"
          />
        )}
      </div>
      <div className="px-3 py-2 flex items-center justify-between text-xs text-gray-600">
        <span>step {capture.step_id ?? "—"}</span>
        <span>{(capture.size_bytes / 1024).toFixed(1)} KB</span>
      </div>
    </a>
  );
}
```

## 검증

### typecheck + build PASS

`npm run typecheck` / `npm run build`.

### 수동 (사용자, 시간 되실 때)

- M3.1.4 에서 만든 실행 중 실패 (raise) 한 것 상세 페이지 진입
- 캡처 섹션에 PNG 썸네일 그리드 표시
- 카드 클릭 → 새 탭에 원본 이미지 (cookies 자동 전송으로 인증 통과)
- (옵션) Railway 재배포 직후 옛 캡처 → 410 Gone → "이미지 사라짐" placeholder

### 코어 회귀 25/25.

## Core / 백엔드 수정

**없음**.

## 범위 밖

- 다중 agent 직접 선택 dropdown (M3.1.6)
- 라이트박스 / 모달 미리보기
- 캡처 다운로드 zip
- 시각적 회전·줌
- 썸네일 사전 생성 (현재 원본 그대로 표시 — 수백 KB라 OK)
