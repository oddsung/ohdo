# ohdo agent — dependencies
# 루트 requirements.txt 와 독립. 에이전트 전용 venv 에서 설치.

# M0
pystray>=0.19.5
Pillow>=10.0.0
httpx>=0.27.0

# M1.5 — WS 클라이언트. sync API 용으로 >=12 필요.
websockets>=12.0

# M2.9.1 — agent 프로세스에서 에러 발생 시 화면 캡처. embedded python 쪽 mss 와 별개.
mss>=10.0
