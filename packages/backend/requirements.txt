# ohdo backend — dependencies
# 루트 requirements.txt 와 독립. 백엔드 전용 venv 에서 설치.

fastapi>=0.110.0
uvicorn[standard]>=0.29.0

# Persistence + migrations (M1.1)
sqlalchemy[asyncio]>=2.0.0
alembic>=1.13.0
asyncpg>=0.29.0
aiosqlite>=0.20.0       # 로컬 개발 SQLite 폴백
pydantic-settings>=2.0.0

# Device Flow + /link 페이지 (M1.2)
jinja2>=3.1.0
python-multipart>=0.0.9
