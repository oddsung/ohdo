{
  "name": "ohdo-devloop",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "ohdo desktop_v3 자율 테스트-개발 루프 하네스 — desktop_v3 와 독립적으로 동작하며 외부에서 빌드·기동·E2E 검증 후 claude --print 로 자율 수정한다.",
  "license": "AGPL-3.0-or-later",
  "scripts": {
    "loop": "tsx src/orchestrator.ts",
    "loop:dry": "tsx src/orchestrator.ts --dry-run",
    "test:e2e": "playwright test",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
